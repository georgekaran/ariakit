---
"@ariakit/react-components": patch
"@ariakit/react": patch
---

Added a `ComboboxInput` alias for the [`Combobox`](https://ariakit.com/reference/combobox) component, along with the `ComboboxInputOptions` and `ComboboxInputProps` types. This makes the input role explicit when composing it alongside an upcoming `ComboboxSelect` component to create a filterable select.
